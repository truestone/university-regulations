<div class="container mx-auto px-4 py-8">
  <div class="max-w-6xl mx-auto">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">규정 임포트 관리</h1>
      <p class="text-gray-600">백그라운드 임포트 작업을 관리하고 진행률을 모니터링합니다.</p>
    </div>

    <!-- 새 임포트 시작 -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-8">
      <h2 class="text-xl font-semibold mb-4">새 임포트 시작</h2>
      
      <div class="flex gap-4">
        <%= link_to "전체 파일 임포트", regulation_imports_path, 
            method: :post, 
            class: "bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg font-medium transition-colors",
            data: { confirm: "전체 규정집 파일을 임포트하시겠습니까? 시간이 오래 걸릴 수 있습니다." } %>
        
        <%= link_to "샘플 파일 테스트", sample_regulation_imports_path, 
            method: :post, 
            class: "bg-green-600 hover:bg-green-700 text-white px-6 py-2 rounded-lg font-medium transition-colors" %>
      </div>
    </div>

    <!-- 현재 실행 중인 작업들 -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-8">
      <h2 class="text-xl font-semibold mb-4">현재 작업 상태</h2>
      
      <% if @import_jobs.any? %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Job ID</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">상태</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">파일</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">시작 시간</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">액션</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% @import_jobs.each do |job| %>
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-mono text-gray-900">
                    <%= link_to job[:job_id][0..7], regulation_import_path(job[:job_id]), 
                        class: "text-blue-600 hover:text-blue-800" %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <% if job[:status] == 'running' %>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                        실행 중
                      </span>
                    <% else %>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                        대기 중
                      </span>
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    <%= File.basename(job[:file_path]) %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    <%= (job[:started_at] || job[:created_at]).strftime("%Y-%m-%d %H:%M:%S") %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                    <%= link_to "상세보기", regulation_import_path(job[:job_id]), 
                        class: "text-blue-600 hover:text-blue-900 mr-3" %>
                    <% if job[:status] == 'queued' %>
                      <%= link_to "취소", cancel_regulation_import_path(job[:job_id]), 
                          method: :delete, 
                          class: "text-red-600 hover:text-red-900",
                          data: { confirm: "작업을 취소하시겠습니까?" } %>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="text-center py-8 text-gray-500">
          현재 실행 중인 작업이 없습니다.
        </div>
      <% end %>
    </div>

    <!-- 최근 완료된 작업들 -->
    <div class="bg-white rounded-lg shadow-md p-6">
      <h2 class="text-xl font-semibold mb-4">최근 완료된 작업</h2>
      
      <% if @recent_results.any? %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Job ID</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">결과</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">레코드 수</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">소요 시간</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">완료 시간</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% @recent_results.each do |result| %>
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-mono text-gray-900">
                    <%= result[:job_id][0..7] if result[:job_id] %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <% if result[:success] %>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                        성공
                      </span>
                    <% else %>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                        실패
                      </span>
                    <% end %>
                    <% if result[:errors_count] > 0 %>
                      <span class="ml-2 text-xs text-gray-500">
                        (에러 <%= result[:errors_count] %>개)
                      </span>
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                    <%= result[:total_records] || 0 %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    <% if result[:duration] %>
                      <%= "#{result[:duration].round(1)}초" %>
                    <% else %>
                      -
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                    <% if result[:completed_at] %>
                      <%= Time.parse(result[:completed_at]).strftime("%Y-%m-%d %H:%M:%S") %>
                    <% else %>
                      -
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="text-center py-8 text-gray-500">
          최근 완료된 작업이 없습니다.
        </div>
      <% end %>
    </div>
  </div>
</div>